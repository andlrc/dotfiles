#!/bin/sh
# Usage:
#	rpglemake <file>
#
# Author: Andreas Louv <andreas@louv.dk>
# Version: 0.7

path=$1; shift
path=$(realpath "$path") # GNU

filename=${path##*/}
project=$(printf '%s\n' "$path" |
	awk -F/ '{ print $(NF-1) }')

ssh dksrv206 "./compile/do '$project' '$filename'" < "$path"
